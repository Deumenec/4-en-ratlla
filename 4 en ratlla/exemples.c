//
//  exemples.c
//  4 en ratlla
//
//  Created by Domènec Huerta Estradé on 11/10/25.
//

#include <stdio.h>
/*
Node* CreateNodeDetallat(char** tauler, int numDeFills, int nivell){
    Node* newNode = malloc(sizeof(Node));
    newNode->tauler=tauler;
    newNode->nivell=nivell;
    newNode->n_fills=numDeFills;
    newNode->qualitat=3.14;
    newNode->fills = malloc(sizeof(Node**)*numDeFills);
    return newNode;
}

void exampleNodeTree (char** partida){
    //Exemple per veure si funcionen correctamentels nodes, no serveix de resss
    Node *exemple = CreateNodeDetallat(partida, 2, 1);
    exemple->fills[0]=CreateNodeDetallat(partida, 1, 2);
    exemple->fills[1]=CreateNodeDetallat(partida, 0, 2);
    exemple->fills[0]->fills[0]=CreateNodeDetallat(partida, 0, 3);
    printNodeTree(exemple);
}
*/
